{
  "name": "@danielkalen/exit-intent",
  "version": "2.0.1",
  "description": "Exit Intent Engine",
  "main": "app.coffee",
  "devDependencies": {
    "coffee-script": "^1.10.0",
    "foreach-cli": "^1.6.1",
    "json": "^9.0.4",
    "node-sass": "^3.4.2",
    "simplyimport": "^2.3.3",
    "simplywatch": "^2.3.2",
    "uglify-js": "^2.6.2"
  },
  "peerDependencies": {
    "jquery": "*",
    "@danielkalen/popup-engine": "^3.0.0"
  },
  "scripts": {
    "postversion": "echo \\'$(json -f package.json version)\\' > .version.coffee && npm run build",
    "build": "npm run build:pretty && npm run build:ugly",
    "build:ugly": "simplyimport -i src/exit-intent.coffee | coffee -b -s -c > dist/js/exit-intent.pretty.js && uglifyjs dist/js/exit-intent.pretty.js -m -c keep_fargs,unused=false -o dist/js/exit-intent.js",
    "build:pretty": "simplyimport -i src/exit-intent.coffee | coffee -b -s -c > dist/js/exit-intent.pretty.js && cp -f dist/js/exit-intent.pretty.js dist/js/exit-intent.js",
    "watch": "simplywatch -g 'src/*' -x 'npm run build:pretty -s'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/danielkalen/exit-intent.git"
  },
  "author": "Daniel Kalen",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/danielkalen/exit-intent/issues"
  },
  "homepage": "https://github.com/danielkalen/exit-intent#readme"
}
